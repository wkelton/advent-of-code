#pragma once

#include <string>
#include <vector>

namespace advent {
namespace strings {

std::vector<std::string> split(const std::string& s, char delim);

} // namespce string
} // namesapce advent